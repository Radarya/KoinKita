# AI Agent Custom Instructions

**CRITICAL FILE DELETION POLICY**
- **NEVER** delete any files from the project without explicit, prior approval from the user. 
- This project syncs with GitHub. If files (such as `public/audio/*`, `assets/icon.png`, `.env`, etc.) are deleted in AI Studio, they will be unexpectedly deleted in the user's GitHub repository upon commit. 
- Always ask for confirmation before using the delete tool or removing assets/files.
